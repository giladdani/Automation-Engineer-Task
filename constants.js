const API_BASE_URL = 'https://api.demoblaze.com';
const USERNAME = "gilad.danino";
const PASSWORD = "p@ssword";    // unsafe
const TIMEOUT = 5000;

module.exports = {
    API_BASE_URL,
    USERNAME,
    PASSWORD,
    TIMEOUT
}